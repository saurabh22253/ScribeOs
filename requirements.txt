# ScribeOS Python dependencies
# Install with:  pip install -r requirements.txt

# UI framework (desktop app via Flet)
flet>=0.21.0

# Google Gemini AI (transcription + MOM generation)
# Uses the current google-genai SDK — NOT the deprecated google-generativeai package
google-genai>=1.0.0

# Secure API-key storage in the macOS Keychain
keyring>=24.3.0

# Optional: load a .env file for the API key during development
python-dotenv>=1.0.1

# PDF export — Markdown → styled PDF
markdown>=3.5.0        # parse Markdown (tables, fenced_code extensions)
reportlab>=4.0.0       # pure-Python PDF generation — no system libs needed
